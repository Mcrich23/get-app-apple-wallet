# === Pass Identity (from your existing .env) ===
PASS_ID=
TEAM_ID=

# === Server ===
PORT=8787
# The public HTTPS URL where this server is reachable (needed for pass auto-updates)
WEB_SERVICE_URL=

# === Auth token for Apple Wallet API requests (pick any long random string) ===
AUTH_TOKEN=CHANGE_ME_TO_A_RANDOM_SECRET

# === Signing certificates ===
# Passphrase for signerKey.pem (leave empty if none)
PASS_PHRASE=

# === GET API Credentials ===
# To generate passes you need valid GET credentials.
# Log in at https://get.cbord.com/ucsc/full/login.php?mobileapp=1
# then use the deviceId and PIN stored by GET Tools.
GET_DEVICE_ID=
GET_PIN=

# === Convex Database ===
# Your Convex deployment URL (e.g., https://your-project-123.convex.cloud)
# Set as a Cloudflare Worker secret: wrangler secret put CONVEX_URL
CONVEX_URL=

# === APNs Push Notifications (for automatic pass refresh) ===
# Set these in the Convex dashboard under Environment Variables.
# APNS_KEY_ID=           # 10-char Key ID from Apple Developer portal
# APNS_TEAM_ID=          # Your Apple Developer Team ID
# APNS_PRIVATE_KEY=      # Contents of the .p8 auth key file (with \n for newlines)
# APNS_ENVIRONMENT=      # "production" or "development" (defaults to "production")

SIGNER_CERT_PEM=
SIGNER_KEY_PEM=
WWDR_PEM=