{
    "name": "get-wallet-server",
    "version": "1.0.0",
    "description": "Apple Wallet pass server for UCSC GET Card barcodes",
    "main": "src/worker.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail"
    },
    "dependencies": {
        "passkit-generator": "^3.2.0"
    },
    "devDependencies": {
        "wrangler": "^4.65.0"
    }
}
